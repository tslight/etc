#+TITLE: Terminal Tools
#+PROPERTY: header-args :cache yes
#+PROPERTY: header-args :comments no
#+PROPERTY: header-args+ :mkdirp yes
#+PROPERTY: header-args+ :padline no
#+PROPERTY: header-args+ :results silent
* GIT
** CONFIG

#+BEGIN_SRC conf :tangle ~/.config/git/config
  [color]
  ui = true
  [diff]
  noprefix = true
  renames = false
  [push]
  default = current
  [alias]
  a = add -A
  b = branch -a
  cl = clone --single-branch --depth 1
  cc = checkout -b
  cb = checkout
  cm = commit -s -m
  d = diff --relative
  f = fetch --all
  ll = log --graph --oneline
  l = log --oneline -n 10
  s = status
  p = pull
  P = push
  [core]
  autocrlf = false
  whitespace = trailing-space,space-before-tab
  [pull]
  rebase = false
  [init]
  defaultBranch = main
  [user]
  name = "~Not Toby"
  email = oxff@sdf.org
#+END_SRC

** IGNORE

#+BEGIN_SRC conf :tangle ~/.config/git/ignore
  .DS_Store
  .localized
  desktop.ini
  ,*.swp
  ,*~
  ,*.o
  ,*.core
  \#*
  .\#*
  TAGS
  tags
#+END_SRC

* NEOVIM

#+begin_src lua :tangle ~/.config/nvim/init.lua
  vim.cmd.colorscheme("quiet")

  vim.g.mapleader = " "
  vim.keymap.set("n", "<leader>w", ':wall', { desc = "Write" })
  vim.keymap.set("n", "<leader>q", ':q!',   { desc = "Quit" })
  vim.keymap.set("n", "<leader>e", ':e ', { desc = "Edit" })
  vim.keymap.set("n", "<leader>b", ':b ', { desc = "Buffers" })
  vim.keymap.set("n", "<leader>n", ':bn<cr>', { desc = "Buffer Next" })
  vim.keymap.set("n", "<leader>p", ':bp<cr>', { desc = "Buffer Prev" })
  vim.keymap.set("n", "<leader>d", ':bd<cr>', { desc = "Buffer Delete" })
  vim.keymap.set("n", "<leader>t", ':terminal<cr>', { desc = "Terminal" })

  vim.opt.cursorline = true
  vim.opt.laststatus=0
  vim.opt.number = true
  vim.opt.relativenumber = true
  vim.opt.undofile = true
  vim.opt.wrap = false
#+end_src

* TMUX

#+BEGIN_SRC conf :tangle ~/.tmux.conf
  set -g mouse on
  # Allow terminal graphics sequences to pass through tmux
  set -g allow-passthrough on
  # Ensure tmux updates terminal environment identifiers
  set -ga update-environment TERM
  set -ga update-environment TERM_PROGRAM
#+END_SRC

#+BEGIN_SRC conf :tangle (if (eq system-type 'berkeley-unix) "~/.tmux.conf" "no")
  set -s copy-command 'xsel -b'
#+END_SRC

#+BEGIN_SRC conf :tangle (if (eq system-type 'darwin) "~/.tmux.conf" "no")
  set -s copy-command 'pbcopy'
#+END_SRC

* YAZI

#+begin_src conf :tangle ~/.config/yazi/yazi.toml
  [mgr]
  # Default is [1, 4, 3]. Change it to give more or less space to the middle pane.
  # Parent = 1 part, Current = 2 parts, Preview = 5 parts
  ratio = [1, 3, 5]
#+end_src
