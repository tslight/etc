#+TITLE: HACK AND BE MERRY!
#+AUTHOR: Toby Slight

#+PROPERTY: header-args :cache yes
#+PROPERTY: header-args+ :mkdirp yes
#+PROPERTY: header-args+ :results silent
#+PROPERTY: header-args+ :padline no
#+PROPERTY: header-args+ :tangle ~/.stumpwm.d/keys.lisp
#+PROPERTY: header-args+ :tangle-mode (identity #o644)

* PREFIX KEY

Decided to stick with the default as having C-i for stump nad C-j for tmux is
too confusing for my hands!

#+BEGIN_SRC common-lisp
  ;; (set-prefix-key (kbd "C-i"))
#+END_SRC

* EMACS MAP

#+BEGIN_SRC common-lisp
  (defvar *my-emacs-map* (make-sparse-keymap))
  (define-key *my-emacs-map* (kbd "e") "emacs")
  (define-key *my-emacs-map* (kbd "C-e") "emacs")
  (define-key *my-emacs-map* (kbd "n") "exec emacs")
  (define-key *my-emacs-map* (kbd "c") "exec emacsclient -c -a ''")
  (define-key *my-emacs-map* (kbd "q") "exec emacs --no-init-file --quick")
  (define-key *my-emacs-map* (kbd "d") "exec emacs --debug-init")
  (define-key *my-emacs-map* (kbd "t") "exec emacs -nw")
#+END_SRC

* TERMINAL MAP

#+BEGIN_SRC common-lisp
  (defvar *my-term-map* (make-sparse-keymap))
  (define-key *my-term-map* (kbd "C-c") "exec x-terminal-emulator -e tmux attach")
  (define-key *my-term-map* (kbd "s") "prompt exec x-terminal-emulator -e ssh ")
  (define-key *my-term-map* (kbd "r") "prompt exec x-terminal-emulator -e ")
#+END_SRC

* BROWSER MAP

#+BEGIN_SRC common-lisp
  (defvar *my-web-map* (make-sparse-keymap))
  (define-key *my-web-map* (kbd "C-w") "exec x-www-browser")
  (define-key *my-web-map* (kbd "c") "chromium")
  (define-key *my-web-map* (kbd "f") "firefox")
  (define-key *my-web-map* (kbd "g") "google")
  (define-key *my-web-map* (kbd "i") "imdb")
  (define-key *my-web-map* (kbd "u") "prompt exec x-www-browser https://")
  (define-key *my-web-map* (kbd "w") "wikipedia")
  (define-key *my-web-map* (kbd "y") "youtube")
#+END_SRC

* SCREEN MAP

#+BEGIN_SRC common-lisp
  (defvar *my-screen-map* (make-sparse-keymap))
  (define-key *my-screen-map* (kbd "n") "snext")
  (define-key *my-screen-map* (kbd "p") "sprev")
  (define-key *my-screen-map* (kbd "o") "sother")
#+END_SRC

* TOP MAP

#+BEGIN_SRC common-lisp
  (define-key *top-map* (kbd "XF86AudioLowerVolume") "volume Master 5%-")
  (define-key *top-map* (kbd "XF86AudioRaiseVolume") "volume Master 5%+")
  (define-key *top-map* (kbd "XF86AudioMute") "volume Master toggle")
  ;; (define-key *top-map* (kbd "XF86MonBrightnessDown") "exec xbacklight -dec 10%")
  ;; (define-key *top-map* (kbd "XF86MonBrightnessUp") "exec xbacklight -inc 10%")
#+END_SRC

#+BEGIN_SRC common-lisp
  ;; windows
  (define-key *top-map* (kbd "C-F4") "delete")
  (define-key *top-map* (kbd "M-F4") "kill")
  (define-key *top-map* (kbd "s-q") "delete")
  (define-key *top-map* (kbd "s-j") "next")
  (define-key *top-map* (kbd "s-k") "prev")
  (define-key *top-map* (kbd "C-s-j") "move-window down")
  (define-key *top-map* (kbd "C-s-k") "move-window up")
  (define-key *top-map* (kbd "C-s-h") "move-window left")
  (define-key *top-map* (kbd "C-s-l") "move-window right")
  (define-key *top-map* (kbd "s-n") "gnext")
  (define-key *top-map* (kbd "s-p") "gprev")
  (define-key *top-map* (kbd "C-s-n") "gnext-with-window")
  (define-key *top-map* (kbd "C-s-p") "gprev-with-window")
  (define-key *top-map* (kbd "C-F11") "fullscreen")
  ;; applications
  (define-key *top-map* (kbd "s-e") "emacs")
  (define-key *top-map* (kbd "s-c") "terminal")
  (define-key *top-map* (kbd "s-w") "web-browser")
  (define-key *top-map* (kbd "s-d") "file-manager")
  (define-key *top-map* (kbd "s-r") "exec rofi -show drun")
  (define-key *top-map* (kbd "s-SPC") "exec rofi -show drun")
  ;; maps
  (define-key *top-map* (kbd "C-s-e") *my-emacs-map*)
  (define-key *top-map* (kbd "C-s-c") *my-term-map*)
  (define-key *top-map* (kbd "C-s-s") *my-screen-map*)
  (define-key *top-map* (kbd "C-s-w") *my-web-map*)
  ;; groups
  (define-key *top-map* (kbd "s-Tab") "gother")
  ;; frames
  (define-key *top-map* (kbd "M-Escape") "fnext")
  (define-key *top-map* (kbd "C-F10") "only")
  (define-key *top-map* (kbd "s-o") "only")
  (define-key *top-map* (kbd "s-a") "exec rofi -show windowcd")
  (define-key *top-map* (kbd "C-s-a") "exec rofi -show window")
  ;; misc
  (define-key *top-map* (kbd "s-m") "mode-line")
  (define-key *top-map* (kbd "C-s-r") "reinit")
  (define-key *top-map* (kbd "C-s-XF86Eject") "exec slock")
  (define-key *top-map* (kbd "C-s-Delete") "exec slock")
  (define-key *top-map* (kbd "C-s-q") "quit")
#+END_SRC

* ROOT MAP

#+BEGIN_SRC common-lisp
  ;; applications
  (define-key *root-map* (kbd "e") "emacs")
  (define-key *root-map* (kbd "c") "terminal")
  (define-key *root-map* (kbd "w") "web-browser")
  (define-key *root-map* (kbd "d") "file-manager")
  (define-key *root-map* (kbd "r") "exec rofi -show drun")
  ;; maps
  (define-key *root-map* (kbd "C-e") '*my-emacs-map*)
  (define-key *root-map* (kbd "C-c") '*my-term-map*)
  (define-key *root-map* (kbd "C-w") '*my-web-map*)
  (define-key *root-map* (kbd "C-s") '*my-screen-map*)
  ;; frames
  (define-key *root-map* (kbd "C-o") "fother")
  (define-key *root-map* (kbd "C-r") "iresize")
  (define-key *root-map* (kbd "u") "remove")
  (define-key *root-map* (kbd "q") "only")
  (define-key *root-map* (kbd "v") "hsplit")
  ;; windows
  (define-key *root-map* (kbd "Tab") "select")
  (define-key *root-map* (kbd "M-r") "reinit")
  (define-key *root-map* (kbd "a") "exec rofi -show windowcd")
  (define-key *root-map* (kbd "C-a") "exec rofi -show window")
  ;; misc
  (define-key *root-map* (kbd "C-l") "exec slock")
  (define-key *root-map* (kbd "M-q") "quit")
  (define-key *root-map* (kbd "m") "mode-line")
  (define-key *root-map* (kbd "S") "swank")
  (define-key *root-map* (kbd "M-s") "sensors")
  (define-key *root-map* (kbd "T") "time")
#+END_SRC

* MENU MAP

#+BEGIN_SRC common-lisp
  (define-key *menu-map* (kbd "TAB") 'menu-down)
  (define-key *menu-map* (kbd "ISO_Left_Tab") 'menu-up)
#+END_SRC

* GROUP MAP

#+BEGIN_SRC common-lisp
  (define-key *groups-map* (kbd "g") "gother")
  (define-key *groups-map* (kbd "C-n") "gnext-with-window")
  (define-key *groups-map* (kbd "C-p") "gprev-with-window")
  (define-key *groups-map* (kbd "l") "grouplist")
  (define-key *groups-map* (kbd "s") "gselect")
#+END_SRC
* RAT MAP

Cycle windows by scrolling on the modeline.

#+BEGIN_SRC common-lisp
  (defun modeline-click (modeline button x y)
    (let* ((screen (parse-integer
                    (ppcre:scan-to-strings
                     "[0-9]+(?=x[0-9]+x[0-9]+)"
                     (write-to-string (current-screen)))))
           (min 50) ;; pixels
           (mid (floor screen 2))
           (max (- screen min)))
      (cond
        ;; left corner of screen
        ((and (eq button 1) (<= x min))
         (run-commands "gprev"))
        ((and (eq button 3) (<= x min))
         (run-commands "gnext"))
        ((and (eq button 5) (<= x min))
         (run-commands "gnext"))
        ((and (eq button 4) (<= x min))
         (run-commands "gprev"))
        ;; left side of screen
        ((and (eq button 1) (> x min) (< x mid))
         (run-commands "prev"))
        ((and (eq button 3) (> x min) (< x mid))
         (run-commands "next"))
        ((and (eq button 4) (> x min) (< x mid))
         (run-commands "prev"))
        ((and (eq button 5) (> x min) (< x mid))
         (run-commands "next"))
        ;; right side of screen
        ((and (eq button 1) (> x mid) (< x max))
         (run-commands "next"))
        ((and (eq button 3) (> x mid) (< x max))
         (run-commands "prev"))
        ((and (eq button 4) (> x mid) (< x max))
         (run-commands "prev"))
        ((and (eq button 5) (> x mid) (< x max))
         (run-commands "next"))
        ;; right corner of screen
        ((and (eq button 1) (>= x max))
         (run-commands "gnext"))
        ((and (eq button 3) (<= x min))
         (run-commands "gprev"))
        ((and (eq button 5) (>= x max))
         (run-commands "gnext"))
        ((and (eq button 4) (>= x max))
         (run-commands "gprev")))))

  (add-hook *mode-line-click-hook* 'modeline-click)
#+END_SRC
